# Restless report AI rules

Follow `AGENTS.md`. The canonical plaintext is private
`yoga-fusion-workspace/report-source/report.json`; ignored
`content/report.json` is legacy only. Validate before encryption, never store a
passphrase in code, and do not push `main` without explicit publication
approval from Andriy.
