# Інструкція для Claude

Обов'язково виконуй `AGENTS.md` у цій теці та
`D:\Unity Projects\AGENTS.md`. `content/report.json` не є джерелом правди;
canonical plaintext зберігається у приватному Yoga Fusion workspace. Не називай
редактор викладачів чинним live: реліз 14.08 є історичним, поточний стан —
підтверджена регресія.
