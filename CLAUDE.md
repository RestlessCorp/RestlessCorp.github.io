# Інструкція для Claude

Обов'язково виконуй `AGENTS.md` у цій теці та
`D:\Unity Projects\AGENTS.md`. `content/report.json` не є джерелом правди;
canonical plaintext зберігається у приватному Yoga Fusion workspace. Не
фіксуй тут поточний статус редактора викладачів: історичну регресію 14.08 треба
зберігати у звіті, а чинний стан і відновлення щоразу брати з canonical
release-state та live production proof.
